# 실습 심화.

# 반복 학습

## 반복 학습 방법
- develop이나 main에 따로 병합하지 않고 stage 별로 브랜치를 나눠 반복 학습을 이어간다.
- 새로운 반복되는 패턴을 실습하는 practice 원격 저장소를 만든다.
- 이 저장소에서는 stage 별로 반복되는 패턴을 연습하며 구조를 익힌다.
- 원본 코드를 보지 않고 해당 구조를 구현할 수 있게 되는 것이 목표이다.
- stage${n}$/${n}$try의 형태로 브랜치를 만든다 EX) stage1/1try
- 반복 학습을 이어가며 브랜치를 커밋할 때 회고록.md파일에 stage${n}$_${n}$try 회고를 남긴다.

---

### 🎓 졸업 판정
* **원본 코드 열람 횟수:** `   `회 (3번 이하 시 졸업)
* [ ] 다음 stage로 넘어가도 되는가?  [✅,❌ 선택]

### 📝 학습 내용
* 이번 stage 반복에서 느낀 어려운 점이나 배운 점을 기록합니다.
---