## Stage 1. 핵심 도메인 모델 — 학습 활동(Activity)

학습 활동은 다음 세 가지 종류 중 하나다: **강의(LECTURE) / 실습(PRACTICE) / 독서(READING)**.

공통 속성:
- 제목 (필수, 공백 불가)
- 학습 시간(분) (필수, 1 이상)
- 공개 여부 (공개/비공개, 필수)
- 생성/수정 시각 (자동 기록, 값 변경 불가하게)

종류별 전용 속성 (해당 종류일 때만 의미 있음):
- 강의: 강사 이름 — 비어 있으면 "강사 미정"으로 자동 채운다.
- 실습: 완료율(%) — 0~100 범위를 벗어나면 경계값으로 보정한다(음수는 0, 100 초과는 100).
- 독서: 책 제목

비즈니스 규칙:
- 제목이나 학습 시간이 유효하지 않은 값으로 들어오면 생성/수정 시점에 막는다.
- 학습 시간을 나중에 늘릴 수 있는 기능이 있다(증가량은 1분 이상이어야 함).
- 공개 상태를 나중에 전환할 수 있다(공개 ↔ 비공개).
- 제목을 나중에 바꿀 수 있다(빈 값 불가).

**설계 과제**: 종류별로 다른 필드를 가지는 하나의 개념을 어떻게 모델링할 것인가? (상속 vs 단일 테이블 vs 다른 방식) — 원본은 특정한 선택을 했지만, 정답은 하나가 아닙니다. 먼저 스스로 결정하고 원본과 비교해보세요.

---

## Stage 2. 태그

- 활동 하나에 여러 개의 태그를 붙일 수 있다.
- 태그는 저장 시 공백 제거, 소문자 변환한다.
- 같은 태그를 중복으로 추가해도 하나만 유지된다.
- 태그를 개별적으로 제거할 수 있다. 없는 태그를 지우려 해도 에러가 아니다.
- 특정 태그가 붙어 있는지 확인할 수 있다.
- 태그는 최대 개수 제한(예: 10개), 각 태그 최대 길이 제한(예: 20자)이 있고, 허용 문자(한글/영문/숫자/일부 특수문자)만 쓸 수 있다.

**API 관점**: 태그는 "활동에 속한 것"이다. 태그 목록 조회/추가/삭제 API의 URL 구조를 이 관계가 드러나도록 설계해보세요.

---

## Stage 3. 사용자(User)와 소유 관계

- 사용자는 닉네임과 이메일(중복 불가)을 가진다.
- 활동은 한 명의 사용자(작성자)에게 속한다. 한 사용자는 여러 활동을 가질 수 있다.
- 사용자를 삭제하면 그 사용자의 활동도 함께 삭제되어야 한다.
- 활동 목록 조회 시, 특정 사용자의 것만 필터링해서 볼 수 있어야 한다.
- 활동 상세 정보에는 작성자의 식별자와 닉네임이 함께 노출되어야 한다(사용자 객체 전체가 아니라 필요한 정보만).
- **(보완)** 이 시점엔 아직 로그인 기능이 없으므로, 작성자를 요청 파라미터 등으로 임시 지정해도 된다. 다만 Stage 13(인가)에서 "클라이언트가 임의로 작성자를 지정할 수 없다"로 바뀐다는 것을 염두에 두고, 작성자 지정 로직을 한 곳에 모아두세요(나중에 바꾸기 쉽게).
- **(보완)** 비밀번호, 권한(역할) 등 인증에 필요한 속성은 이 단계에서는 다루지 않는다 — Stage 12에서 확장한다.

---

## Stage 4. 입력 검증

- 활동 생성 시 다음이 지켜져야 한다:
  - 종류(type) 필수
  - 제목: 필수, 100자 이하
  - 학습 시간: 1~1440분 사이
  - 공개 여부: 필수
  - 학습한 날짜(선택): 미래 날짜 불가
- **종류별 필수 필드 검증**: 강의는 강사 이름, 실습은 완료율, 독서는 책 제목이 반드시 채워져야 한다. (다른 종류의 필드는 없어도 됨)
- 검증 실패 시 어떤 필드가 왜 잘못됐는지 클라이언트가 알 수 있는 형태로 응답해야 한다.
- **(보완)** 태그 목록(Stage 2)에 대한 검증(최대 개수, 최대 길이, 허용 문자)도 활동 생성/태그 추가 요청 시점에 함께 적용되어야 한다 — 검증 로직을 도메인(Stage 1~2)과 요청 DTO 중 어디에 둘지, 혹은 양쪽에 걸쳐 이중으로 둘지 스스로 정해보세요.

**설계 과제**: 종류별 필수 필드 검증처럼 "여러 필드의 조합"을 검증하는 로직을 어디에, 어떤 방식으로 넣을 것인가?

---

## Stage 5. 예외 처리 체계

- 애플리케이션에서 발생 가능한 에러를 종류별로 분류하고, 각각 고유 코드/HTTP 상태/기본 메시지를 갖게 한다. 예:
  - 활동을 찾을 수 없음 → 404
  - 입력값이 유효하지 않음 → 400
  - 존재하지 않는 경로 요청 → 404
  - 예상 못한 서버 오류 → 500 (원인은 로그에만 남기고, 클라이언트에는 안전한 메시지만 노출)
- 모든 에러 응답은 일관된 형식(에러 코드, 메시지, 발생 시각, 요청 경로 등)을 가져야 한다.
- 검증 실패 시 응답에는 어떤 필드가 문제였는지 필드별 메시지가 포함되어야 한다.
- **(보완, 미리 자리만 잡아둘 것)** 아직 인증/인가 기능은 없지만, 에러 코드 체계를 설계할 때 "인증 안 됨"과 "권한 없음"을 나중에 추가할 여지를 남겨두세요(코드 접두사 규칙 등). 이미 있는 코드 체계를 Stage 13에서 갈아엎게 되면 그게 신호입니다 — 처음부터 확장 가능하게 짜보세요.

**설계 과제**: "예외 클래스 계층"과 "에러 코드 체계"를 분리할지 합칠지 결정해보세요. 예외 하나 늘 때마다 여러 곳을 고쳐야 한다면 구조를 의심해보세요.

---

## Stage 6. 조회 기능 — 목록, 페이징, 검색

- 활동 목록을 페이지 단위로 조회할 수 있다. 정렬 기준(예: id, 학습시간, 제목)과 정렬 방향(오름/내림차순)을 선택할 수 있다. 아무것도 지정하지 않으면 합리적인 기본값(예: 최신순)이 적용된다.
- "전체 개수를 알아야 하는 페이징"과 "다음 페이지 존재 여부만 알면 되는 무한스크롤형 페이징"을 구분해서 제공한다(공개 활동만 대상으로).
- 다음 조건으로 활동을 검색할 수 있다: 종류별, 제목 키워드 포함(대소문자 무시), 최소 학습시간 이상.
- 태그로 필터링해서 조회할 수 있다.
- 카테고리별로 그룹화된 목록을 조회할 수 있다.
- 전체/강의/실습/독서 개수 요약 정보를 조회할 수 있다.
- 모든 태그를 알파벳순으로 모아서 조회할 수 있다.
- **(보완)** 위 조회 기능들은 기본적으로 "공개(PUBLIC) 활동"만 대상으로 한다. 비공개 활동은 이 시점(로그인 기능 도입 전)에는 아예 조회 대상에서 제외한다. Stage 13(인가)에서 "본인 소유의 비공개 활동은 본인에게만 보여야 한다"로 규칙이 확장된다는 것을 미리 염두에 두고, 조회 조건에 "공개 여부"를 하드코딩하지 말고 파라미터화해두세요.
- **(보완)** 페이지 크기의 최소/최대값(예: 1~100)을 제한해서, 클라이언트가 비정상적으로 큰 페이지를 요청하지 못하게 하세요.

---

## Stage 7. 활동 CRUD API

- 활동 생성: 성공 시 201, 생성된 자원 위치 정보 포함.
- 활동 단건 조회: 없으면 404.
- 활동 수정: 제목과 공개 여부만 변경 가능. 없으면 404, 성공 시 200.
- 활동 삭제: 성공 시 204(본문 없음), 없으면 404. 첨부 파일이 있었다면 함께 정리되어야 한다.
- 삭제는 여러 번 호출해도 결과가 같아야 한다는 원칙(멱등성)을 어디에 적용할지 생각해보세요(태그 삭제 API에도 해당).
- **(보완)** 비공개 활동을 단건 조회할 때, 이 시점(인가 도입 전)에는 누구나 조회 가능하게 임시로 열어두어도 됩니다. 다만 이 부분을 나중에(Stage 13) 어디서 막을지 — Controller? Service? — 미리 표시해두세요(TODO 주석 등).

---

## Stage 8. 파일 첨부

- 활동 생성 시 파일을 함께 첨부할 수 있다(선택).
- 허용 확장자는 화이트리스트 방식으로 제한한다(이미지/문서/텍스트 계열만 허용).
- **(보완)** 파일 크기 제한을 둔다(예: 최대 10MB). 제한을 넘는 파일 업로드 시 명확한 에러(413 또는 400)로 거부한다.
- 저장 파일명은 원본과 무관하게 추측 불가능한 이름으로 저장한다(원본 확장자는 유지).
- 첨부 파일 "보기" 요청과 "다운로드" 요청을 구분해서 제공한다(다운로드는 브라우저가 저장하도록 응답).
- 활동 삭제 시 첨부 파일도 함께 삭제되어야 한다.
- 저장 위치는 로컬 디스크와 외부 스토리지(S3 등) 중 설정으로 전환 가능해야 한다 — 둘 다 같은 방식으로 호출하는 쪽(컨트롤러/서비스)은 코드를 안 바꿔도 되게.

**설계 과제**: "저장 방식이 바뀌어도 호출부는 안 바뀐다"를 만족하는 구조를 스스로 고민해보세요. (인터페이스 추출)

**보안 고려사항 (직접 챙겨볼 것)**:
- 업로드 파일명에 경로 조작 문자가 섞여 있어도 안전해야 한다(디렉터리 탈출 방지).
- 다운로드 요청도 마찬가지로 지정된 저장 폴더 밖의 파일에 접근할 수 없어야 한다.
- **(보완)** 업로드된 파일의 실제 내용(MIME 타입)이 확장자와 다른 경우(예: `.jpg`인데 실제로는 실행 파일)를 어디까지 검증할지 정책을 정해보세요. 확장자 검사만으로 충분한지, 파일 시그니처(매직 넘버) 검사까지 필요한지 스스로 판단해보세요.

---

## Stage 9. 변경 이력(감사 로그)

- 활동에 대한 주요 작업(생성/수정/삭제 등)이 발생하면 "무슨 작업이었는지 + 상세 설명"을 이력으로 남긴다.
- 이력은 최근 순으로 조회할 수 있다.
- **(보완)** 이력에는 "누가 수행했는지"도 함께 남아야 의미가 있다. 이 시점엔 로그인 기능이 없으므로 임시 값(예: 고정 사용자, 또는 요청 파라미터로 받은 사용자)을 기록해두고, Stage 12(인증) 도입 후 실제 로그인한 사용자 정보로 교체하세요.
- **(보완)** 이력 조회 API도 페이징이 필요합니다(Stage 6에서 만든 페이징 방식 중 하나를 재사용해보세요).
- **(보완, 인가 도입 후 되짚어볼 것)** 이력에는 다른 사용자의 활동 정보도 포함될 수 있습니다. 이 이력 조회 API를 누구나 볼 수 있게 둘 것인지, 관리자만 볼 수 있게 제한할 것인지 Stage 13에서 반드시 결정하세요.

---

## Stage 10. 트랜잭션 원자성/전파 학습용 데모 (선택, 심화)

이 스테이지는 실무 기능이 아니라 **트랜잭션 개념을 눈으로 확인하기 위한 실습**입니다. 아래 세 가지 시나리오를 각각 별도 엔드포인트로 만들어서, 실패 시 무엇이 롤백되고 무엇이 남는지 직접 확인해보세요.

1. **원자성 데모**: 활동 저장 + 이력 기록을 "하나의" 트랜잭션으로 묶는다. 중간에 실패를 강제로 발생시키면 활동과 이력 둘 다 사라져야 한다.
2. **전파 데모**: "시도 이력"은 실제 등록 성공 여부와 무관하게 항상 남기고 싶다. 본 등록 작업이 실패해도 "시도했다"는 기록만은 독립적으로 커밋되게 만들어보세요. (힌트: 트랜잭션 전파 옵션 중 하나가 이걸 가능하게 합니다. 같은 클래스 안에서 메서드를 호출하면 왜 안 되는지도 함께 확인해보세요.)
3. **체크 예외와 롤백 데모**: 특정 체크 예외(Exception을 직접 상속)를 던졌을 때, 스프링의 기본 롤백 정책이 이 예외에도 적용되는지 확인해보세요. 기본 동작과 예상이 다르다면 왜 다른지, 어떻게 명시적으로 지정하는지 찾아보세요.

---

## Stage 11. 부가 기능 (선택, 시간 되면)

- API 문서 자동화(Swagger/OpenAPI) 붙이기.
- 응답에 관련 리소스로 이동할 수 있는 링크 정보 포함하기(HATEOAS) — 예: 활동 상세 조회 응답에 "이 활동의 태그 목록" 링크 포함.
- 메서드 실행 시간을 로깅하는 공통 기능(관점지향, AOP) 만들어보기.
- 커스텀 지표(예: 생성된 활동 수, 누적 학습 시간)를 수집해서 모니터링 도구로 노출하기(자세한 내용은 Stage 15에서 이어서 다룹니다).
- **(보완)** API를 여러 버전으로 나눠서 운영해야 하는 상황을 가정해보세요(예: 응답 필드 이름을 바꾸고 싶은데 기존 클라이언트는 그대로 둬야 함). 기존 버전은 그대로 두고 새 버전을 추가하되, 기존 버전에는 "곧 지원 종료된다"는 신호를 응답에 포함시켜 보세요.

---

## Stage 12. 인증(Authentication) — 회원가입과 로그인

- 사용자는 이메일과 비밀번호로 회원가입할 수 있다.
- 이메일은 중복 가입이 불가능하다 — 이미 가입된 이메일이면 명확한 오류를 반환한다(Stage 5 에러 체계에 새 코드로 편입).
- 비밀번호는 원문 그대로 저장하지 않는다(저장 전 반드시 안전한 방식으로 변환해야 한다).
- 비밀번호는 최소한의 안전 기준(예: 8~20자)을 만족해야 하며, 위반 시 회원가입이 거부된다.
- 로그인은 이메일+비밀번호 조합으로 이루어지며, 성공하면 이후 요청에서 "누구인지"를 식별할 수 있는 수단이 주어져야 한다(수단의 형태는 스스로 정해볼 것 — 이 프로젝트에서는 Stage 13에서 우선 한 가지 방식으로 구현하고, Stage 18에서 세션 방식으로 다시 구현하며 비교합니다).
- 잘못된 이메일/비밀번호로 로그인 시, 어떤 값이 틀렸는지 세세히 알려주지 않는다(계정 존재 여부 추측 방지 — "이메일 또는 비밀번호가 올바르지 않습니다" 같은 통합 메시지 사용).
- 로그인 성공/실패 이벤트를 서버 로그로 남긴다(누가, 언제, 성공/실패 여부).

**설계 과제**: 인증 방식(세션 vs 토큰)을 정하기 전에, "인증이 안 된 사용자"와 "인증은 됐지만 권한이 없는 사용자"를 구분해서 서로 다른 응답 코드(401/403)를 줄 수 있는 구조를 먼저 만들어보세요.

---

## Stage 13. 인가(Authorization) — 권한과 소유권 기반 접근 제어

- 사용자는 역할(일반 사용자/관리자)을 가진다.
- 인증되지 않은 사용자는 활동 생성/수정/삭제를 할 수 없다. 조회는 공개 활동에 한해 로그인 없이도 허용한다.
- 활동 생성 시, 로그인한 사용자가 자동으로 작성자(owner)가 된다 — 클라이언트가 요청 본문에 작성자를 임의로 지정해도 무시되어야 한다(Stage 3에서 만들어둔 임시 지정 로직을 여기서 실제 로그인 사용자 기준으로 교체).
- 활동 수정/삭제는 작성자 본인만 가능하다 — 단, 관리자는 예외적으로 모든 활동에 접근 가능하다.
- 비공개 활동은 작성자 본인과 관리자에게만 조회된다(Stage 6에서 파라미터화해둔 "공개 여부 조건"을 로그인 사용자 기준으로 확장).
- 본인 소유가 아닌 자원에 대한 수정/삭제 시도는, 자원이 실제 존재하는지 여부와 무관하게 일관된 거부 응답을 준다(자원 존재 여부 추측 방지 — 403으로 통일할지 404로 숨길지 스스로 정책을 정하고 이유를 설명할 수 있어야 한다).
- Stage 9의 감사 이력 조회 API는 관리자만 접근 가능하도록 제한한다.

**API 관점**: URL 경로/메서드 조합별로 "이 요청은 누가 호출할 수 있는가"를 표로 먼저 정리한 뒤 구현하세요.
예: `POST /activities` → 로그인한 사용자, `DELETE /activities/{id}` → 작성자 본인 또는 관리자, `GET /audit-logs` → 관리자.

**설계 과제**: "소유권 확인" 로직이 여러 API(수정, 삭제, 파일 다운로드 등)에서 반복된다면 어떻게 재사용할지 고민해보세요.

---

## Stage 14. 테스트 전략

- 도메인 객체(Activity 등)의 비즈니스 규칙(Stage 1, 2의 검증/보정 로직)은 외부 의존성 없이 단위 테스트로 검증한다.
- 데이터 조회/검색 로직(Stage 6)은 실제 DB(또는 인메모리 DB)를 띄운 상태에서, 저장된 데이터 기준으로 결과가 맞는지 검증한다.
- 서비스 계층 로직은 외부 의존성을 가짜(mock)로 대체해서, "이 상황에서 이 메서드가 호출되었는가"와 "이 상황에서 이런 예외가 나는가"를 검증한다.
- API 계층은 실제 서버를 띄우지 않고도 HTTP 요청/응답 형태(상태 코드, 응답 바디, 검증 실패 메시지)를 검증할 수 있어야 한다.
- 최소 한 가지 시나리오는 "요청 → 저장 → 응답"까지 전체 계층을 실제로 통과시켜 검증한다(첨부파일이 포함된 시나리오 권장).
- 인증/인가 규칙(Stage 12, 13)에 대해, 다음을 각각 테스트로 증명한다:
  - 로그인하지 않은 요청은 거부된다(401).
  - 본인 소유가 아닌 자원에 대한 수정/삭제는 거부된다(403 또는 정한 정책대로).
  - 관리자는 타인의 자원에도 접근 가능하다.
- 새 기능 하나(예: 주간 학습 목표 달성률 계산)는 테스트를 먼저 작성한 뒤 구현하는 순서로 진행해본다.

**설계 과제**: 테스트 종류별로 "어디까지 실제로 띄우고 어디부터 가짜로 대체할 것인가" 경계선을 스스로 그어보세요. 매번 전체 서버를 띄워서 테스트하면 무엇이 손해인지 생각해보세요.

---

## Stage 15. 운영 준비 — 로깅과 모니터링

- 운영 환경에서는 콘솔이 아니라 파일로 로그가 남아야 한다.
- 로그 레벨 정책을 정한다: 정상적인 생성/수정/삭제는 정보 수준, 예상 가능한 실패(검증 실패, 인가 거부 등)는 경고, 예상 못한 서버 오류는 에러 수준.
- 외부에서 "이 서버가 살아있는지" 확인할 수 있는 방법이 있어야 한다.
- 자체적으로 정의한 상태 체크 항목(예: 첨부파일 저장소 접근 가능 여부)을 이 확인 방법에 포함시킨다.
- 서비스의 핵심 지표(예: 누적 생성된 활동 수, 카테고리별 개수, 특정 작업의 처리 시간)를 외부에서 조회할 수 있게 노출한다.
- 민감한 운영 정보(예: 상세 스택 트레이스, 내부 설정값, 비밀번호 관련 설정)는 이 모니터링 정보에 노출되지 않아야 한다.

---

## Stage 16. 컨테이너화와 배포

- 애플리케이션을 어떤 환경에서든 동일하게 실행할 수 있도록 이미지로 만든다.
- 이미지에는 빌드에만 필요한 도구/파일이 남지 않아야 한다(최종 실행에 필요한 것만 포함).
- 운영 환경에서 사용할 설정값(DB 접속 정보, 외부 스토리지 키 등)은 이미지 안에 하드코딩하지 않고, 실행 시점에 환경변수 등으로 주입한다.
- 애플리케이션과 함께 필요한 부가 요소(DB 등)를 한 번에 기동할 수 있어야 한다.
- 로컬 파일 저장이 아니라 외부 스토리지를 쓰는 설정으로 전환했을 때도, 재기동/재배포와 무관하게 기존 첨부파일이 유지되어야 한다.

**설계 과제**: "여러 서버 인스턴스가 동시에 떠 있어도 문제없이 동작하는가?" 관점에서 지금까지 만든 기능(특히 파일 저장, 로그인 상태 유지)을 점검해보세요. 어느 인스턴스로 요청이 가도 결과가 같아야 합니다.

---

## Stage 17. 지속적 통합/배포(CI/CD)

- 코드가 저장소에 반영될 때마다 자동으로 빌드와 전체 테스트(Stage 14)가 실행되어야 한다.
- 테스트 중 하나라도 실패하면 이후 단계(배포)로 진행되지 않아야 한다.
- 테스트 실행 환경은 실제 운영 설정과 분리되어야 한다(운영 DB/스토리지에 직접 연결되면 안 됨).
- 빌드와 테스트를 모두 통과한 결과물만 배포 파이프라인으로 전달된다.
- 배포 이후, 실제로 새 버전이 반영되었는지 확인할 수 있는 수단이 있어야 한다(예: Stage 15에서 만든 상태 확인 API에 버전 정보 포함).

---

## Stage 18. 세션 기반 로그인으로 전환 (심화, 선택)

- Stage 12~13에서 만든 인증/인가 방식이 세션 기반이 아니었다면, 세션 기반 로그인을 별도로 구현해서 두 방식의 차이를 직접 비교해본다.
- 로그인 성공 시 서버가 로그인 상태를 기억하는 방식이며, 클라이언트는 매 요청마다 자격증명을 다시 보내지 않아도 된다.
- 로그인 상태 유지 수단(쿠키)에는 최소한의 보안 속성(스크립트로 탈취 불가, 통신 구간 보호 등)이 적용되어야 한다.
- 일정 시간 활동이 없으면 로그인 상태가 자동으로 만료되어야 한다.
- 로그아웃 시 즉시 로그인 상태가 무효화되어야 한다(로그아웃 이후 같은 쿠키로 요청해도 인증된 것으로 처리되면 안 됨).
- 동일 계정으로 여러 곳에서 동시 로그인했을 때의 정책을 스스로 정해본다(허용? 이전 세션 자동 만료?).
- 로그인/로그아웃, 세션 생성/만료 시점에 이벤트를 기록해서 Stage 15의 로그 정책에 편입시킨다.

**설계 과제**: 세션 방식과 이전에 만든 인증 방식 각각의 장단점을, 이 프로젝트에서 실제로 겪은 문제(예: Stage 16의 여러 서버 인스턴스 환경)를 근거로 비교해서 정리해보세요.

---

## 진행 방식 제안

1. Stage 1~7까지는 원본을 보지 않고 최대한 스스로 설계/구현.
2. 막히는 지점(예: "종류별 다른 필드를 가진 엔티티 설계")에서만 원본의 **해당 클래스 하나만** 열어서 확인.
3. Stage 완료마다 전체 목록 대비 API 스펙(요청/응답 형태)을 직접 적어보고, 원본의 응답 구조와 비교.
4. Stage 8(파일 첨부)과 Stage 12~13(인증/인가)부터는 난이도가 확 올라갑니다 — 이 지점들은 막히면 부담 없이 원본을 참고하며 진행해도 괜찮아요. 이 프로젝트의 핵심 학습 포인트는 Stage 1~7의 "구조 설계", 그리고 Stage 13의 "권한 판단을 어디에 둘 것인가" 쪽입니다.
5. Stage 14(테스트)는 각 Stage를 끝낼 때마다 그때그때 조금씩 쌓아가는 것을 권장합니다 — 맨 마지막에 몰아서 하면 "왜 이 로직이 이렇게 짜여 있었는지" 기억이 흐려집니다.
6. Stage 16~17(배포/CI-CD)은 로컬에서 기능이 다 동작한 뒤에 진행해도 무방합니다. 순서를 바꿔서 먼저 익혀도 상관없습니다.
7. 다 끝나면 같은 도메인(학습 활동 기록)을 조금 다른 요구사항으로 — 예: "활동에 댓글 추가", "역할을 3단계로 세분화" — 한 번 더 처음부터 설계해보세요. 이게 진짜 백지 훈련입니다.