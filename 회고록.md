# Stage`1` / `2`TRY

---
### 🎓 졸업 판정
* **원본 코드 열람 횟수:** `0`회 (3번 이하 시 졸업)
* [❌] 다음 stage로 넘어가도 되는가?  [✅,❌ 선택]

### 📝 학습 내용
## [아쉬운 점]
>이번 stage`1`/ `2`TRY에서 느낀 어려운 점이나 배운 점을 기록합니다.  
  
이번 try에서는 문법적인 어려움이나 어노테이션 등을 기억 못해서 겪은 어려움은 없었다.  
다만, 검토 과정에서 자잘한 실수들을 찾지 못 하여 심각은 버그를 만들었다.   
 ```
private static void validateMinutes(int minutes) {
    if (minutes < 0) {
        throw new IllegalArgumentException("학습 시간은 1분 이상이여야 합니다.");
    }
}
```
- 버그: validateMinutes 경계값 오류  
요구사항은 1 이상만을 유효한 값으로 정의했지만, 코드는 음수만을 유효하지 않은 값을로 구현하여 0도 유효한 값이되었다.  
  if (minutes < 0) {...}를 if (minutes < 1) {...}로 변경 해야한다.

```angular2html
public String instructorName;
public Integer completionRate;
public String bookTitle;
```
- 심각한 설계 문제: instructorName, completionRate, bookTitle 필드가 public이다.  
이는 캡슐화가 완전이 깨지는 심각한 오류이다. 필드를 `public`으로 선언하여 외부애서   
`activity.instructorName = "아무값";` 이런 식으로 해당 필드에 아무 값이나 넣을 수 있는 상태이다.  
필드는 특별한 이유가 없다면 반드시 `private`으로 선언했는지 확인하자.

> ## [긍정 평가]
> - static 메서드 판단 전부 정확했다.
> - 정규화 로직 (강사명/완료율/책제목) 전부 정확했다.
> - 상태변경 메서드들 다 public으로 정확했다. (1차 때 실수한게 반영됨)
> - increaseMinutes에서 validateMinutes를 재사용한 것도 좋은 판단이다.

---
